<article class="destination-content">
  <slot />
</article>

<style>
  .destination-content {
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(249, 250, 251, 0.96));
    border: 1px solid rgba(226, 232, 240, 0.9);
    border-radius: 1.5rem;
    box-shadow:
      0 18px 45px rgba(15, 23, 42, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.7);
    padding: 1.75rem;
    color: #334155;
  }

  :global(.dark) .destination-content {
    background:
      linear-gradient(180deg, rgba(17, 24, 39, 0.96), rgba(17, 24, 39, 0.9));
    border-color: rgba(71, 85, 105, 0.5);
    box-shadow:
      0 20px 50px rgba(2, 6, 23, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    color: #d1d5db;
  }

  .destination-content :global(> :first-child) {
    margin-top: 0;
  }

  .destination-content :global(> :last-child) {
    margin-bottom: 0;
  }

  .destination-content :global(h1),
  .destination-content :global(h2),
  .destination-content :global(h3),
  .destination-content :global(h4) {
    color: #0f172a;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.15;
  }

  :global(.dark) .destination-content :global(h1),
  :global(.dark) .destination-content :global(h2),
  :global(.dark) .destination-content :global(h3),
  :global(.dark) .destination-content :global(h4) {
    color: #f8fafc;
  }

  .destination-content :global(h1) {
    font-size: clamp(2rem, 4vw, 2.75rem);
    margin: 0 0 1.25rem;
  }

  .destination-content :global(h2) {
    font-size: clamp(1.35rem, 2vw, 1.8rem);
    margin: 2rem 0 0.9rem;
    padding-top: 1.2rem;
    position: relative;
  }

  .destination-content :global(h2)::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 4rem;
    height: 4px;
    border-radius: 999px;
    background: linear-gradient(90deg, #eab308, #f59e0b);
  }

  .destination-content :global(h3) {
    font-size: 1.2rem;
    margin: 1.4rem 0 0.75rem;
  }

  .destination-content :global(h4) {
    font-size: 1rem;
    margin: 1.1rem 0 0.65rem;
  }

  .destination-content :global(p) {
    font-size: 1.02rem;
    line-height: 1.9;
    margin: 0 0 1rem;
    color: inherit;
  }

  .destination-content :global(p > strong:only-child) {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    margin-bottom: 0.5rem;
    padding: 0.55rem 0.9rem;
    border-radius: 999px;
    background: rgba(234, 179, 8, 0.12);
    color: #92400e;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  :global(.dark) .destination-content :global(p > strong:only-child) {
    background: rgba(250, 204, 21, 0.14);
    color: #fde68a;
  }

  .destination-content :global(strong) {
    color: #111827;
    font-weight: 800;
  }

  :global(.dark) .destination-content :global(strong) {
    color: #ffffff;
  }

  .destination-content :global(ul),
  .destination-content :global(ol) {
    margin: 1rem 0 1.35rem;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.7rem;
  }

  .destination-content :global(li) {
    position: relative;
    margin: 0;
    padding: 0.95rem 1rem 0.95rem 3rem;
    border-radius: 1rem;
    background: rgba(248, 250, 252, 0.9);
    border: 1px solid rgba(226, 232, 240, 0.95);
    box-shadow: 0 8px 24px rgba(15, 23, 42, 0.05);
    line-height: 1.7;
  }

  :global(.dark) .destination-content :global(li) {
    background: rgba(30, 41, 59, 0.7);
    border-color: rgba(71, 85, 105, 0.6);
    box-shadow: none;
  }

  .destination-content :global(ul li)::before,
  .destination-content :global(ol li)::before {
    content: "";
    position: absolute;
    left: 1rem;
    top: 1rem;
    width: 1.1rem;
    height: 1.1rem;
    border-radius: 999px;
    background: linear-gradient(135deg, #facc15, #f59e0b);
    box-shadow: 0 0 0 4px rgba(250, 204, 21, 0.14);
  }

  .destination-content :global(hr) {
    border: 0;
    height: 1px;
    margin: 2rem 0;
    background: linear-gradient(90deg, transparent, rgba(148, 163, 184, 0.45), transparent);
  }

  .destination-content :global(blockquote) {
    margin: 1.5rem 0;
    padding: 1rem 1.2rem 1rem 1.1rem;
    border-left: 4px solid #f59e0b;
    border-radius: 0 1rem 1rem 0;
    background: rgba(255, 251, 235, 0.75);
    color: #78350f;
  }

  :global(.dark) .destination-content :global(blockquote) {
    background: rgba(120, 53, 15, 0.18);
    color: #fde68a;
  }

  .destination-content :global(a) {
    color: #b45309;
    font-weight: 700;
    text-decoration: none;
    border-bottom: 1px solid rgba(180, 83, 9, 0.3);
  }

  :global(.dark) .destination-content :global(a) {
    color: #fbbf24;
    border-bottom-color: rgba(251, 191, 36, 0.35);
  }

  @media (min-width: 1024px) {
    .destination-content {
      padding: 2.25rem;
    }
  }
</style>
