---
import { Image } from 'astro:assets';
---

<section class="hero">
	<div class="hero-background">
		<div class="hero-overlay"></div>
	</div>
	
	<div class="container">
		<div class="hero-content">
			<div class="text-content">
				<h1 class="hero-title">
					Descubra o mundo com a
					<span class="highlight">Partiu Viagens!</span>
				</h1>
				
				<p class="hero-description">
					Descubra o mundo com a nossa agência de viagens! Especializados
					em excursões inesquecíveis, oferecemos roteiros personalizados e
					experiências únicas. Explore novos destinos, mergulhe em culturas
					fascinantes e crie memórias duradouras. Venha viajar com a gente!
					✈️
				</p>
				
				<button class="cta-button" id="explorarDestinosBtn">
					<span class="button-icon">✈️</span>
					EXPLORAR DESTINOS
				</button>
			</div>
			
			<div class="mascot-section">
				<div class="mascot-container">
					<Image 
						src="/src/assets/mascote_pv.png"
						alt="Mascote Partiu Viagens"
						width={500}
						height={500}
						class="mascot-image"
					/>
					
					<!-- Elementos decorativos -->
					<div class="location-tag chile">
						<span class="location-icon">📍</span>
						<span>CHILE</span>
					</div>
					
					<div class="location-tag iguacu">
						<span class="location-icon">📍</span>
						<span>FOZ IGUAÇU</span>
					</div>
					
					<div class="floating-element phone">
						<div class="phone-screen">
							<div class="phone-content">
								<span class="currency">$</span>
								<div class="location-pin">📍</div>
							</div>
						</div>
					</div>
					
					<div class="floating-element map">
						<div class="map-content">
							<div class="map-lines"></div>
							<div class="map-pin">📍</div>
						</div>
					</div>
				</div>
				
				<div class="send-button">
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none">
						<path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z" fill="white"/>
					</svg>
				</div>
			</div>
		</div>
	</div>
</section>

<!-- Modal de Seleção de Destinos -->
<div id="destinosModal" class="modal-overlay">
	<div class="modal-content">
		<div class="modal-header">
			<h2 class="modal-title">Escolha seu destino</h2>
			<button id="closeModal" class="modal-close-button">
				&times;
			</button>
		</div>
		<div class="modal-body">
			<a href="/destinos/nacionais" class="destination-card national">
				<span class="destination-icon">🇧🇷</span>
				<h3 class="destination-title">Destinos Nacionais</h3>
				<p class="destination-description">Explore as belezas do Brasil</p>
			</a>
			<a href="/destinos/internacionais" class="destination-card international">
				<span class="destination-icon">🌍</span>
				<h3 class="destination-title">Destinos Internacionais</h3>
				<p class="destination-description">Descubra o mundo</p>
			</a>
		</div>
	</div>
</div>

<style>
	/* Estilos do Modal */
	.modal-overlay {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		z-index: 1002;
		display: none; /* Alterado para none por padrão */
		align-items: center;
		justify-content: center;
		background-color: rgba(0, 0, 0, 0.6);
		backdrop-filter: blur(5px);
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.modal-overlay.visible {
		display: flex;
		opacity: 1;
	}

	.modal-content {
		position: relative;
		margin: 1rem;
		width: 100%;
		max-width: 500px;
		max-height: 90vh;
		overflow-y: auto;
		background-color: white;
		border-radius: 16px;
		box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
		transform: scale(0.95);
		transition: transform 0.3s ease;
	}

	.modal-overlay.visible .modal-content {
		transform: scale(1);
	}

	.modal-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 1.5rem;
		border-bottom: 1px solid #e5e7eb;
	}

	.modal-title {
		font-size: 1.25rem;
		font-weight: 700;
		color: #1f2937;
	}

	.modal-close-button {
		background: none;
		border: none;
		font-size: 2rem;
		line-height: 1;
		color: #6b7280;
		cursor: pointer;
		transition: color 0.2s ease, transform 0.2s ease;
	}

	.modal-close-button:hover {
		color: #1f2937;
		transform: rotate(90deg);
	}

	.modal-body {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		padding: 1.5rem;
	}

	.destination-card {
		flex: 1;
		padding: 1.5rem;
		border-radius: 12px;
		text-align: center;
		text-decoration: none;
		transition: transform 0.3s ease, box-shadow 0.3s ease;
		border: 2px solid transparent;
	}

	.destination-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
	}

	.destination-icon {
		display: block;
		margin-bottom: 1rem;
		font-size: 3rem;
	}

	.destination-title {
		margin-bottom: 0.5rem;
		font-size: 1.125rem;
		font-weight: 600;
		color: #1f2937;
	}

	.destination-description {
		font-size: 0.875rem;
		color: #4b5563;
	}

	.national {
		background: linear-gradient(135deg, #f0fdf4, #dcfce7);
		border-color: #4ade80;
	}

	.international {
		background: linear-gradient(135deg, #eff6ff, #dbeafe);
		border-color: #60a5fa;
	}

	@media (min-width: 640px) {
		.modal-body {
			flex-direction: row;
		}
	}

	.hero {
		position: relative;
		min-height: 100vh;
		display: flex;
		align-items: center;
		overflow: hidden;
		padding-top: 100px; /* Account for fixed header */
		-webkit-overflow-scrolling: touch;
		touch-action: manipulation;
	}

	.hero-background {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(135deg, #e0f2fe 0%, #f3e5f5 50%, #fff3e0 100%);
		z-index: -2;
	}

	.hero-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="%23ffffff" stroke-width="0.5" opacity="0.3"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>') repeat;
		opacity: 0.3;
		z-index: -1;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 2rem;
		width: 100%;
	}

	.hero-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		min-height: 80vh;
	}

	.text-content {
		max-width: 500px;
	}

	.hero-title {
		font-size: 3.5rem;
		font-weight: 800;
		line-height: 1.1;
		color: #1f2937;
		margin-bottom: 1.5rem;
	}

	.highlight {
		color: #1e3a8a;
		position: relative;
	}

	.hero-description {
		font-size: 1.1rem;
		line-height: 1.6;
		color: #4b5563;
		margin-bottom: 2.5rem;
	}

	.cta-button {
		background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
		color: white;
		border: none;
		padding: 1rem 2rem;
		border-radius: 50px;
		font-size: 1rem;
		font-weight: 600;
		letter-spacing: 0.5px;
		cursor: pointer;
		transition: all 0.3s ease;
		display: flex;
		align-items: center;
		gap: 0.5rem;
		box-shadow: 0 8px 25px rgba(30, 58, 138, 0.3);
	}

	.cta-button:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 35px rgba(30, 58, 138, 0.4);
	}

	.button-icon {
		font-size: 1.2rem;
	}

	.mascot-section {
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.mascot-container {
		position: relative;
		width: 400px;
		height: 400px;
	}

	.mascot-image {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 20px;
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
	}

	.location-tag {
		position: absolute;
		background: white;
		padding: 0.5rem 1rem;
		border-radius: 20px;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 0.85rem;
		font-weight: 600;
		color: #374151;
		animation: float 3s ease-in-out infinite;
	}

	.chile {
		top: 20%;
		left: -10%;
		animation-delay: 0s;
	}

	.iguacu {
		bottom: 20%;
		right: -15%;
		animation-delay: 1.5s;
	}

	.floating-element {
		position: absolute;
		background: white;
		border-radius: 12px;
		box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
		padding: 1rem;
		animation: float 4s ease-in-out infinite;
	}

	.phone {
		top: 10%;
		right: -20%;
		width: 60px;
		height: 80px;
		animation-delay: 0.5s;
	}

	.map {
		bottom: 10%;
		left: -15%;
		width: 80px;
		height: 60px;
		animation-delay: 2s;
	}

	.phone-screen {
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, #3b82f6 0%, #1e3a8a 100%);
		border-radius: 8px;
		display: flex;
		align-items: center;
		justify-content: center;
		color: white;
	}

	.phone-content {
		text-align: center;
	}

	.currency {
		font-size: 1.5rem;
		font-weight: bold;
	}

	.location-pin {
		font-size: 0.8rem;
	}

	.map-content {
		width: 100%;
		height: 100%;
		position: relative;
		background: #f0f9ff;
		border-radius: 8px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.map-lines {
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		width: 30px;
		height: 20px;
		background: linear-gradient(45deg, #3b82f6 2px, transparent 2px),
					linear-gradient(-45deg, #3b82f6 2px, transparent 2px);
		background-size: 8px 8px;
		opacity: 0.3;
	}

	.map-pin {
		font-size: 1.2rem;
		z-index: 1;
	}

	.send-button {
		position: absolute;
		bottom: -20px;
		right: 20px;
		width: 60px;
		height: 60px;
		background: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 8px 25px rgba(139, 92, 246, 0.3);
		cursor: pointer;
		transition: transform 0.3s ease;
		animation: pulse 2s infinite;
	}

	.send-button:hover {
		transform: scale(1.1);
	}

	@keyframes float {
		0%, 100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-10px);
		}
	}

	@keyframes pulse {
		0% {
			box-shadow: 0 8px 25px rgba(139, 92, 246, 0.3);
		}
		50% {
			box-shadow: 0 8px 25px rgba(139, 92, 246, 0.6);
		}
		100% {
			box-shadow: 0 8px 25px rgba(139, 92, 246, 0.3);
		}
	}

	@media (max-width: 768px) {
		.hero {
			padding-top: 120px;
			min-height: auto;
			padding-bottom: 2rem;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 3rem;
			text-align: center;
			min-height: auto;
		}

		.text-content {
			order: 2;
			max-width: none;
			margin-top: 1rem;
		}

		.mascot-section {
			order: 1;
			margin-bottom: 2rem;
		}

		.hero-title {
			font-size: 2.5rem;
			margin-bottom: 1rem;
		}

		.hero-description {
			margin-bottom: 1.5rem;
		}

		.cta-button {
			margin: 0 auto;
			max-width: 280px;
		}

		.mascot-container {
			width: 300px;
			height: 300px;
			margin: 0 auto;
		}

		.location-tag {
			position: relative;
			margin: 0.5rem;
			display: inline-flex;
		}

		.chile {
			top: auto;
			left: auto;
		}

		.iguacu {
			bottom: auto;
			right: auto;
		}

		.floating-element {
			display: none;
		}

		.send-button {
			display: none;
		}
	}

	@media (max-width: 480px) {
		.hero {
			padding-top: 100px;
			padding-bottom: 3rem;
		}

		.hero-content {
			gap: 1.5rem;
			display: flex;
			flex-direction: column;
		}

		.hero-title {
			font-size: 2rem;
			margin-bottom: 1rem;
		}

		.hero-description {
			margin-bottom: 2rem;
		}

		.container {
			padding: 0 1rem;
		}

		.mascot-container {
				width: 250px;
				height: 250px;
				margin-bottom: 2rem;
			}

		.mascot-section {
			margin-bottom: 2rem;
			order: 1;
		}

		.text-content {
			margin-top: 1rem;
			order: 2;
			padding-top: 1rem;
		}
		}

	@media (max-width: 375px) {
		.hero-content {
			gap: 1rem;
		}

		.mascot-container {
			width: 220px;
			height: 220px;
			margin-bottom: 1.5rem;
		}

		.mascot-section {
			margin-bottom: 1.5rem;
		}

		.text-content {
			padding-top: 1.5rem;
		}

		.hero-title {
			font-size: 1.8rem;
		}
		}

	/* Media query para dispositivos em landscape ou com altura pequena */
	@media (max-height: 600px) and (orientation: landscape) {
		.hero {
			padding-top: 80px;
			padding-bottom: 2rem;
			min-height: auto;
		}

		.hero-content {
			gap: 1rem;
			min-height: auto;
		}

		.mascot-container {
			width: 200px;
			height: 200px;
		}

		.hero-title {
			font-size: 1.8rem;
			margin-bottom: 0.5rem;
		}

		.hero-description {
			margin-bottom: 1rem;
			font-size: 1rem;
		}
	}

	/* Media query específica para dispositivos móveis reais */
	@media only screen and (max-device-width: 480px) {
		.hero {
			padding-top: 90px;
			padding-bottom: 2rem;
			min-height: 100vh;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 1.5rem;
			display: flex;
			flex-direction: column;
			text-align: center;
			min-height: auto;
			padding: 1rem 0;
		}

		.mascot-section {
			order: 1;
			margin-bottom: 2rem;
			flex-shrink: 0;
		}

		.text-content {
			order: 2;
			max-width: none;
			margin-top: 1rem;
			padding-top: 1rem;
			flex-shrink: 0;
		}

		.mascot-container {
			width: 280px;
			height: 280px;
			margin: 0 auto 1.5rem auto;
		}

		.hero-title {
			font-size: 2rem;
			margin-bottom: 1rem;
			line-height: 1.2;
		}

		.hero-description {
			margin-bottom: 2rem;
			font-size: 1rem;
			line-height: 1.5;
		}

		.cta-button {
			margin: 0 auto;
			max-width: 280px;
		}
	}


	</style>

<script>
	document.addEventListener('DOMContentLoaded', function() {
		const explorarBtn = document.getElementById('explorarDestinosBtn');
		const modal = document.getElementById('destinosModal');
		const closeBtn = document.getElementById('closeModal');

		function openModal() {
			if (!modal) return;
			modal.classList.add('visible');
			document.body.style.overflow = 'hidden';
		}

		function closeModal() {
			if (!modal) return;
			modal.classList.remove('visible');
			document.body.style.overflow = '';
		}

		// Abrir modal
		if (explorarBtn) {
			explorarBtn.addEventListener('click', openModal);
		}

		// Fechar modal com botão X
		if (closeBtn) {
			closeBtn.addEventListener('click', closeModal);
		}

		// Fechar modal clicando fora do conteúdo
		if (modal) {
			modal.addEventListener('click', function(e) {
				if (e.target === modal) {
					closeModal();
				}
			});
		}

		// Fechar modal com tecla ESC
		document.addEventListener('keydown', function(e) {
			if (e.key === 'Escape' && modal.classList.contains('visible')) {
				closeModal();
			}
		});
	});
</script>